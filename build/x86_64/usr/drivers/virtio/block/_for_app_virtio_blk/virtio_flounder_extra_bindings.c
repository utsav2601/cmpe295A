/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: virtio
 * INTEFACE FILE: ../if/virtio.if
 * INTERFACE DESCRIPTION: VirtIO Interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

/*
 * Generated Stub for RPC
 */

#include <barrelfish/barrelfish.h>
#include <flounder/flounder_support.h>
#include <if/virtio_rpcclient_defs.h>

/*
 * RPC wrapper functions
 */
static  errval_t virtio_open__rpc(struct virtio_rpc_client *_rpc, uint8_t backend, virtio_errval_t *err, struct capref *devframe)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).open_call)(_rpc->b, NOP_CONT, backend));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct virtio_binding *_binding = _rpc->b;
    *err = (((_binding->rx_union).open_response).err);
    *devframe = (((_binding->rx_union).open_response).devframe);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t virtio_close__rpc(struct virtio_rpc_client *_rpc)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).close_call)(_rpc->b, NOP_CONT));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t virtio_add__rpc(struct virtio_rpc_client *_rpc, uint16_t vq_id, uint16_t ndesc, uint8_t has_buffers, struct capref vring, virtio_errval_t *err)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).add_call)(_rpc->b, NOP_CONT, vq_id, ndesc, has_buffers, vring));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct virtio_binding *_binding = _rpc->b;
    *err = (((_binding->rx_union).add_response).err);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t virtio_extend__rpc(struct virtio_rpc_client *_rpc, uint16_t vq_id, struct capref vbuf, virtio_errval_t *err)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).extend_call)(_rpc->b, NOP_CONT, vq_id, vbuf));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct virtio_binding *_binding = _rpc->b;
    *err = (((_binding->rx_union).extend_response).err);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t virtio_req__rpc(struct virtio_rpc_client *_rpc, uint64_t size, virtio_errval_t *err, struct capref *vring)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).req_call)(_rpc->b, NOP_CONT, size));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct virtio_binding *_binding = _rpc->b;
    *err = (((_binding->rx_union).req_response).err);
    *vring = (((_binding->rx_union).req_response).vring);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}


/*
 * Receive handlers
 */
static  void virtio_open__rpc_rx_handler(struct virtio_binding *_binding, virtio_errval_t err, struct capref devframe)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).open_response).err = err;
    ((_binding->rx_union).open_response).devframe = devframe;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void virtio_close__rpc_rx_handler(struct virtio_binding *_binding)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void virtio_add__rpc_rx_handler(struct virtio_binding *_binding, virtio_errval_t err)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).add_response).err = err;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void virtio_extend__rpc_rx_handler(struct virtio_binding *_binding, virtio_errval_t err)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).extend_response).err = err;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void virtio_req__rpc_rx_handler(struct virtio_binding *_binding, virtio_errval_t err, struct capref vring)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).req_response).err = err;
    ((_binding->rx_union).req_response).vring = vring;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}


/*
 * RPC Vtable
 */
static  struct virtio_rpc_vtbl virtio_rpc_vtbl = {
    .open = virtio_open__rpc,
    .close = virtio_close__rpc,
    .add = virtio_add__rpc,
    .extend = virtio_extend__rpc,
    .req = virtio_req__rpc,
};

/*
 * Error handler
 */
static  void virtio_rpc_client_error(struct virtio_binding *_binding, errval_t _err)
{
    // get RPC client state pointer
    struct virtio_rpc_client *_rpc = _binding->st;
    
    if (_rpc->rpc_in_progress) {
        assert(err_is_fail(_err));
        _rpc->async_error = _err;
        // kick waitset with dummy event
        flounder_support_register(&(_rpc->rpc_waitset), &(_rpc->dummy_chanstate), dummy_event_closure, true);
    } else {
        USER_PANIC_ERR(_err, "async error in RPC");
    }
}


/*
 * Init function
 */
 errval_t virtio_rpc_client_init(struct virtio_rpc_client *rpc, struct virtio_binding *binding)
{
    errval_t _err;
    
    // Setup state of RPC client object
    rpc->b = binding;
    rpc->reply_present = false;
    rpc->rpc_in_progress = false;
    rpc->async_error = SYS_ERR_OK;
    waitset_init(&(rpc->rpc_waitset));
    flounder_support_waitset_chanstate_init(&(rpc->dummy_chanstate));
    rpc->vtbl = virtio_rpc_vtbl;
    binding->st = rpc;
    
    // Change waitset on binding
    _err = ((binding->change_waitset)(binding, &(rpc->rpc_waitset)));
    if (err_is_fail(_err)) {
        waitset_destroy(&(rpc->rpc_waitset));
        return(err_push(_err, FLOUNDER_ERR_CHANGE_WAITSET));
    }
    
    // Set RX handlers on binding object for RPCs
    (binding->rx_vtbl).open_response = virtio_open__rpc_rx_handler;
    (binding->rx_vtbl).close_response = virtio_close__rpc_rx_handler;
    (binding->rx_vtbl).add_response = virtio_add__rpc_rx_handler;
    (binding->rx_vtbl).extend_response = virtio_extend__rpc_rx_handler;
    (binding->rx_vtbl).req_response = virtio_req__rpc_rx_handler;
    
    // Set error handler on binding object
    binding->error_handler = virtio_rpc_client_error;
    
    return(SYS_ERR_OK);
}

