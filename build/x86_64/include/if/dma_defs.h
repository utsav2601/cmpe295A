#ifndef __dma_IF_H
#define __dma_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: dma
 * INTEFACE FILE: ../if/dma.if
 * INTERFACE DESCRIPTION: Generic DMA service interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t dma_genpaddr_t;
typedef uint64_t dma_genvaddr_t;
typedef uint32_t dma_rsrcid_t;
typedef uint64_t dma_errval_t;
typedef uint64_t dma_cycles_t;
typedef uint32_t dma_iref_t;
typedef uint8_t dma_coreid_t;
typedef uint32_t dma_domainid_t;
typedef uint64_t dma_id_t;

/*
 * Forward declaration of binding type
 */
struct dma_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void dma_bind_continuation_fn(void *st, errval_t err, struct dma_binding *_binding);
typedef  bool dma_can_send_fn(struct dma_binding *_binding);
typedef  errval_t dma_register_send_fn(struct dma_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t dma_change_waitset_fn(struct dma_binding *_binding, struct waitset *ws);
typedef  errval_t dma_control_fn(struct dma_binding *_binding, idc_control_t control);
typedef  void dma_error_handler_fn(struct dma_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum dma_msg_enum {
    dma___dummy__msgnum = 0,
    dma___bind__msgnum = 1,
    dma___bind_reply__msgnum = 2,
    dma_register_call__msgnum = 3,
    dma_register_response__msgnum = 4,
    dma_deregister_call__msgnum = 5,
    dma_deregister_response__msgnum = 6,
    dma_memcpy_call__msgnum = 7,
    dma_memcpy_response__msgnum = 8,
    dma_done__msgnum = 9
} dma_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t dma_register_call__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory);
typedef  errval_t dma_register_response__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr);
typedef  errval_t dma_deregister_call__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory);
typedef  errval_t dma_deregister_response__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr);
typedef  errval_t dma_memcpy_call__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, uint64_t src, uint64_t dst, uint64_t length);
typedef  errval_t dma_memcpy_response__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t err, dma_id_t id);
typedef  errval_t dma_done__tx_method_fn(struct dma_binding *_binding, struct event_closure _continuation, dma_id_t id, dma_errval_t err);

/*
 * Message type signatures (receive)
 */
typedef  void dma_register_call__rx_method_fn(struct dma_binding *_binding, struct capref memory);
typedef  void dma_register_response__rx_method_fn(struct dma_binding *_binding, dma_errval_t msgerr);
typedef  void dma_deregister_call__rx_method_fn(struct dma_binding *_binding, struct capref memory);
typedef  void dma_deregister_response__rx_method_fn(struct dma_binding *_binding, dma_errval_t msgerr);
typedef  void dma_memcpy_call__rx_method_fn(struct dma_binding *_binding, uint64_t src, uint64_t dst, uint64_t length);
typedef  void dma_memcpy_response__rx_method_fn(struct dma_binding *_binding, dma_errval_t err, dma_id_t id);
typedef  void dma_done__rx_method_fn(struct dma_binding *_binding, dma_id_t id, dma_errval_t err);

/*
 * Struct type for holding the args for each msg
 */
struct dma_register_call__args {
    struct capref memory;
};
struct dma_register_response__args {
    dma_errval_t msgerr;
};
struct dma_deregister_call__args {
    struct capref memory;
};
struct dma_deregister_response__args {
    dma_errval_t msgerr;
};
struct dma_memcpy_call__args {
    uint64_t src;
    uint64_t dst;
    uint64_t length;
};
struct dma_memcpy_response__args {
    dma_errval_t err;
    dma_id_t id;
};
struct dma_done__args {
    dma_id_t id;
    dma_errval_t err;
};

/*
 * Union type for all message arguments
 */
union dma_arg_union {
    struct dma_register_call__args register_call;
    struct dma_register_response__args register_response;
    struct dma_deregister_call__args deregister_call;
    struct dma_deregister_response__args deregister_response;
    struct dma_memcpy_call__args memcpy_call;
    struct dma_memcpy_response__args memcpy_response;
    struct dma_done__args done;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct dma_tx_vtbl {
    dma_register_call__tx_method_fn *register_call;
    dma_register_response__tx_method_fn *register_response;
    dma_deregister_call__tx_method_fn *deregister_call;
    dma_deregister_response__tx_method_fn *deregister_response;
    dma_memcpy_call__tx_method_fn *memcpy_call;
    dma_memcpy_response__tx_method_fn *memcpy_response;
    dma_done__tx_method_fn *done;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct dma_rx_vtbl {
    dma_register_call__rx_method_fn *register_call;
    dma_register_response__rx_method_fn *register_response;
    dma_deregister_call__rx_method_fn *deregister_call;
    dma_deregister_response__rx_method_fn *deregister_response;
    dma_memcpy_call__rx_method_fn *memcpy_call;
    dma_memcpy_response__rx_method_fn *memcpy_response;
    dma_done__rx_method_fn *done;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t dma_connect_fn(void *st, struct dma_binding *binding);

/*
 * Export state struct
 */
struct dma_export {
    struct idc_export common;
    dma_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t dma_export(void *st, idc_export_callback_fn *export_cb, dma_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The message buffer structure (for accept/connect)
 */
struct dma_frameinfo {
    /* Physical address of send buffer */
    lpaddr_t sendbase;
    
    /* Pointer to incoming message buffer */
    void *inbuf;
    
    /* Size of the incoming buffer in bytes */
    size_t inbufsize;
    
    /* Pointer to outgoing message buffer */
    void *outbuf;
    
    /* Size of the outgoing buffer in bytes */
    size_t outbufsize;
    
};

/*
 * Accept function over already shared frame
 */
extern  errval_t dma_accept(struct dma_frameinfo *_frameinfo, void *st, dma_bind_continuation_fn *_continuation, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct dma_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    dma_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    dma_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    dma_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    dma_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    dma_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct dma_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct dma_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union dma_arg_union tx_union;
    union dma_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum dma_msg_enum tx_msgnum;
    enum dma_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    dma_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t dma_bind(iref_t i, dma_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Generic connect function over already shared frame
 */
extern  errval_t dma_connect(struct dma_frameinfo *_frameinfo, dma_bind_continuation_fn *_continuation, void *st, struct waitset *ws, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t dma_register_call__tx(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory) __attribute__ ((always_inline));
static inline errval_t dma_register_call__tx(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory)
{
    return(((_binding->tx_vtbl).register_call)(_binding, _continuation, memory));
}

static inline errval_t dma_register_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr) __attribute__ ((always_inline));
static inline errval_t dma_register_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr)
{
    return(((_binding->tx_vtbl).register_response)(_binding, _continuation, msgerr));
}

static inline errval_t dma_deregister_call__tx(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory) __attribute__ ((always_inline));
static inline errval_t dma_deregister_call__tx(struct dma_binding *_binding, struct event_closure _continuation, struct capref memory)
{
    return(((_binding->tx_vtbl).deregister_call)(_binding, _continuation, memory));
}

static inline errval_t dma_deregister_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr) __attribute__ ((always_inline));
static inline errval_t dma_deregister_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t msgerr)
{
    return(((_binding->tx_vtbl).deregister_response)(_binding, _continuation, msgerr));
}

static inline errval_t dma_memcpy_call__tx(struct dma_binding *_binding, struct event_closure _continuation, uint64_t src, uint64_t dst, uint64_t length) __attribute__ ((always_inline));
static inline errval_t dma_memcpy_call__tx(struct dma_binding *_binding, struct event_closure _continuation, uint64_t src, uint64_t dst, uint64_t length)
{
    return(((_binding->tx_vtbl).memcpy_call)(_binding, _continuation, src, dst, length));
}

static inline errval_t dma_memcpy_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t err, dma_id_t id) __attribute__ ((always_inline));
static inline errval_t dma_memcpy_response__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_errval_t err, dma_id_t id)
{
    return(((_binding->tx_vtbl).memcpy_response)(_binding, _continuation, err, id));
}

static inline errval_t dma_done__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_id_t id, dma_errval_t err) __attribute__ ((always_inline));
static inline errval_t dma_done__tx(struct dma_binding *_binding, struct event_closure _continuation, dma_id_t id, dma_errval_t err)
{
    return(((_binding->tx_vtbl).done)(_binding, _continuation, id, err));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/dma_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/dma_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/dma_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/dma_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __dma_IF_H
