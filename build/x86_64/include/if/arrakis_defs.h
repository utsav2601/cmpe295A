#ifndef __arrakis_IF_H
#define __arrakis_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: arrakis
 * INTEFACE FILE: ../if/arrakis.if
 * INTERFACE DESCRIPTION: Interface to arrakismon
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t arrakis_genpaddr_t;
typedef uint64_t arrakis_genvaddr_t;
typedef uint32_t arrakis_rsrcid_t;
typedef uint64_t arrakis_errval_t;
typedef uint64_t arrakis_cycles_t;
typedef uint32_t arrakis_iref_t;
typedef uint8_t arrakis_coreid_t;
typedef uint32_t arrakis_domainid_t;

/*
 * Forward declaration of binding type
 */
struct arrakis_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void arrakis_bind_continuation_fn(void *st, errval_t err, struct arrakis_binding *_binding);
typedef  bool arrakis_can_send_fn(struct arrakis_binding *_binding);
typedef  errval_t arrakis_register_send_fn(struct arrakis_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t arrakis_change_waitset_fn(struct arrakis_binding *_binding, struct waitset *ws);
typedef  errval_t arrakis_control_fn(struct arrakis_binding *_binding, idc_control_t control);
typedef  void arrakis_error_handler_fn(struct arrakis_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum arrakis_msg_enum {
    arrakis___dummy__msgnum = 0,
    arrakis___bind__msgnum = 1,
    arrakis___bind_reply__msgnum = 2,
    arrakis_spawn_arrakis_domain_call__msgnum = 3,
    arrakis_spawn_arrakis_domain_response__msgnum = 4
} arrakis_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t arrakis_spawn_arrakis_domain_call__tx_method_fn(struct arrakis_binding *_binding, struct event_closure _continuation, const char *path, const char *argvbuf, size_t argvbytes, const char *envbuf, size_t envbytes);
typedef  errval_t arrakis_spawn_arrakis_domain_response__tx_method_fn(struct arrakis_binding *_binding, struct event_closure _continuation, arrakis_errval_t err, arrakis_domainid_t domain_id);

/*
 * Message type signatures (receive)
 */
typedef  void arrakis_spawn_arrakis_domain_call__rx_method_fn(struct arrakis_binding *_binding, char *path, char *argvbuf, size_t argvbytes, char *envbuf, size_t envbytes);
typedef  void arrakis_spawn_arrakis_domain_response__rx_method_fn(struct arrakis_binding *_binding, arrakis_errval_t err, arrakis_domainid_t domain_id);

/*
 * Struct type for holding the args for each msg
 */
struct arrakis_spawn_arrakis_domain_call__args {
    char *path;
    char *argvbuf;
    size_t argvbytes;
    char *envbuf;
    size_t envbytes;
};
struct arrakis_spawn_arrakis_domain_response__args {
    arrakis_errval_t err;
    arrakis_domainid_t domain_id;
};

/*
 * Union type for all message arguments
 */
union arrakis_arg_union {
    struct arrakis_spawn_arrakis_domain_call__args spawn_arrakis_domain_call;
    struct arrakis_spawn_arrakis_domain_response__args spawn_arrakis_domain_response;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct arrakis_tx_vtbl {
    arrakis_spawn_arrakis_domain_call__tx_method_fn *spawn_arrakis_domain_call;
    arrakis_spawn_arrakis_domain_response__tx_method_fn *spawn_arrakis_domain_response;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct arrakis_rx_vtbl {
    arrakis_spawn_arrakis_domain_call__rx_method_fn *spawn_arrakis_domain_call;
    arrakis_spawn_arrakis_domain_response__rx_method_fn *spawn_arrakis_domain_response;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t arrakis_connect_fn(void *st, struct arrakis_binding *binding);

/*
 * Export state struct
 */
struct arrakis_export {
    struct idc_export common;
    arrakis_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t arrakis_export(void *st, idc_export_callback_fn *export_cb, arrakis_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The message buffer structure (for accept/connect)
 */
struct arrakis_frameinfo {
    /* Physical address of send buffer */
    lpaddr_t sendbase;
    
    /* Pointer to incoming message buffer */
    void *inbuf;
    
    /* Size of the incoming buffer in bytes */
    size_t inbufsize;
    
    /* Pointer to outgoing message buffer */
    void *outbuf;
    
    /* Size of the outgoing buffer in bytes */
    size_t outbufsize;
    
};

/*
 * Accept function over already shared frame
 */
extern  errval_t arrakis_accept(struct arrakis_frameinfo *_frameinfo, void *st, arrakis_bind_continuation_fn *_continuation, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct arrakis_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    arrakis_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    arrakis_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    arrakis_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    arrakis_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    arrakis_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct arrakis_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct arrakis_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union arrakis_arg_union tx_union;
    union arrakis_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum arrakis_msg_enum tx_msgnum;
    enum arrakis_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    arrakis_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t arrakis_bind(iref_t i, arrakis_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Generic connect function over already shared frame
 */
extern  errval_t arrakis_connect(struct arrakis_frameinfo *_frameinfo, arrakis_bind_continuation_fn *_continuation, void *st, struct waitset *ws, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t arrakis_spawn_arrakis_domain_call__tx(struct arrakis_binding *_binding, struct event_closure _continuation, const char *path, const char *argvbuf, size_t argvbytes, const char *envbuf, size_t envbytes) __attribute__ ((always_inline));
static inline errval_t arrakis_spawn_arrakis_domain_call__tx(struct arrakis_binding *_binding, struct event_closure _continuation, const char *path, const char *argvbuf, size_t argvbytes, const char *envbuf, size_t envbytes)
{
    return(((_binding->tx_vtbl).spawn_arrakis_domain_call)(_binding, _continuation, path, argvbuf, argvbytes, envbuf, envbytes));
}

static inline errval_t arrakis_spawn_arrakis_domain_response__tx(struct arrakis_binding *_binding, struct event_closure _continuation, arrakis_errval_t err, arrakis_domainid_t domain_id) __attribute__ ((always_inline));
static inline errval_t arrakis_spawn_arrakis_domain_response__tx(struct arrakis_binding *_binding, struct event_closure _continuation, arrakis_errval_t err, arrakis_domainid_t domain_id)
{
    return(((_binding->tx_vtbl).spawn_arrakis_domain_response)(_binding, _continuation, err, domain_id));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/arrakis_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/arrakis_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/arrakis_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/arrakis_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __arrakis_IF_H
